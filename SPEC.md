# 統計トリ🦜 仕様書

個人や仲間内で「ちょっとした統計」を記録・分析・(将来的に)共有するアプリ。

## 1. コンセプト

- 日常のできごと(八百屋の混み具合、バスの遅れ、天気と体調など)をワンタップで記録する。
- 記録がたまると「いつ・どんな時にそうなりやすいか」の傾向・予想を自動で表示する。
- 難しい設定は不要。タイトルとボタンを決めるだけで統計が始められる。

## 2. プラットフォーム方針

**「使えること」を最優先** とするため、ネイティブアプリ(iPhoneアプリ / Androidアプリ)は作らない。

- **PWA(プログレッシブ・ウェブアプリ)として実装する。**
  - iPhone の Safari / Android の Chrome のどちらでも動く。
  - 「ホーム画面に追加」すればアプリのように起動できる(全画面・アイコン付き)。
  - App Store / Google Play の審査・開発者登録が不要。
- 技術構成(想定): HTML + CSS + JavaScript の単一ページアプリ。
  - データ保存は当面 **端末内(localStorage / IndexedDB)**。サーバー不要で即使える。
  - 将来の共有機能導入時にクラウド同期を追加する(→ §8)。

## 3. 画面構成

### 3.1 ホーム画面(起動時)

```
┌──────────────────────┐
│      統計トリ🦜        │  ← アプリタイトル
├──────────────────────┤
│  [ ＋ 新規統計 ]       │  ← 新しい統計を作るボタン
├──────────────────────┤
│  八百屋の混み           │  ← 既存の統計(カード)
│  [混んでる] [混んでない] │  ← 設定したボタンがそのまま並ぶ
│  記録数: 42 最終: 7/1  │
├──────────────────────┤
│  バスの遅れ            │
│  [遅れた] [時間通り]    │
│  記録数: 15 最終: 6/28 │
└──────────────────────┘
```

- タイトル「統計トリ🦜」を最上部に表示。
- その下に「新規統計」ボタン。
- 既存の統計がカード形式で並ぶ。各カードには **設定したボタンがそのまま表示され、ホーム画面から直接ワンタップで記録できる**。
- カードをタップすると詳細画面(§3.3)へ。

### 3.2 新規統計作成画面

入力項目は2つだけ。

| 項目 | 説明 | 例 |
|------|------|-----|
| タイトル | 統計の名前 | 八百屋の混み |
| ボタン | 選択肢(2個以上、追加可能) | 混んでる / 混んでない |

- ボタンは最低2個、「＋選択肢を追加」で3個以上も設定可能(例: 空いてる/普通/混んでる)。
- 各ボタンに色を割り当てる(自動でもよいが、変更可能)。
- 保存するとホーム画面にカードが追加される。

### 3.3 統計詳細画面

上から順に:

1. **タイトル**(例: 八百屋の混み)
2. **記録ボタン**(混んでる / 混んでない)— 押すと現在日時で1件記録し、軽いフィードバック(「記録したトリ🦜」等)を表示。
3. **予想表示 + 精度自信メーター**(§5)— 「今までの結果から、混んでる時間の予想」と、その自信度メーター
4. **グラフ / 集計**(§6)— 表示切り替えタブ付き
5. **記録履歴** — 直近の記録一覧。誤タップ用に **個別削除・修正** が可能。

### 3.4 統計の管理

- 統計ごとに: 名前変更 / ボタンの追加・変更 / 削除(確認ダイアログ付き)。
- 過去の記録の日時修正(「さっき押し忘れた」に対応するため、記録時に日時をさかのぼって入力もできる)。

### 3.5 クイック記録 — 記録までのタップ数を最小にする(最重要)

ユーザーは死ぬほどめんどくさがり、という前提で設計する。**記録までのタップ数が本アプリの生命線。**

| 経路 | タップ数 | 説明 |
|------|---------|------|
| ホーム画面カードのボタン | 起動 + 1タップ | カードにボタンが直接並んでいるので、詳細画面を開かずに記録できる(§3.1) |
| 統計ごとの専用URL | 起動 + 1タップ | `.../#/s/<統計ID>` を開くと、その統計の記録ボタンだけがドンと出る画面に直行 |
| **ボタンごとの専用URL(記録即完了)** | **起動のみ・0タップ** | `.../#/s/<統計ID>/hit/<選択肢>` を開くと **開いた瞬間に記録が完了** し、「記録したトリ🦜」+ 予想を表示。誤爆用に「取り消す」ボタンを数秒表示 |

- URL はすべて分けておき、**スマホのホーム画面にアイコンとして追加できる**。
  - 例: 「混んでる🈵」「混んでない✅」の2アイコンをホーム画面に置けば、**アイコンを押すだけで統計が取れる**。
  - iPhone(Safariの「ホーム画面に追加」)/ Android(Chromeの「ホーム画面に追加」)両対応。
- PWA のアプリアイコン長押しショートカット(Android)にもよく使う統計を登録する。
- 0タップ記録の画面には「実は今回は逆だった」用に **他の選択肢ボタンも小さく併記** し、押すと記録を差し替える。

## 4. 記録データ

1回の記録で保存する内容:

| フィールド | 内容 |
|-----------|------|
| 統計ID | どの統計か |
| 選択肢 | 押したボタン(例: 混んでる) |
| 日時 | 記録した日時(修正可能) |
| メモ(任意) | 一言メモ(例: 「特売日だった」) |

## 5. 予想機能と精度自信メーター

蓄積された記録から「その選択肢になりやすい条件」を計算して表示する。

### 5.1 予想の表示

- 例: 「**混んでる**のは **土曜の10時〜12時** が多いです(80%)」
- **記録1件目から必ず予想を表示する。**「データ不足」のような出し渋りはしない。データが少なくても現時点のベスト予想をあえて言い切る(例: 記録2件でも「今のところ 火曜の夕方 は混んでるかも!」)。
- 計算方法(v1はシンプルに):
  - 時間帯(1〜2時間刻み)× 曜日でグループ化し、各グループでの選択肢の割合を出す。
  - 割合が高い順に上位を「予想」として表示。
- 「今」に対する予想も表示: 詳細画面を開いた時、現在の曜日・時間帯に該当する過去傾向を出す(例: 「今の時間帯は 混んでる 確率 70%」)。

### 5.2 精度自信メーター

予想の信頼度は「隠す」のではなく「**育てる**」見せ方にする。記録するたびにメーターが上がっていくのが見えて、ワクワクして続けたくなることを狙う。

```
予想: 混んでるのは 土曜の10時〜12時!
自信メーター 🐣▓▓▓▓░░░░░░ 38%  (あと5回記録すると自信アップ!)
```

- **自信度の計算(v1)**: 記録の総件数と、時間帯・曜日のカバー率(どれだけ色々な時間帯のデータが集まったか)から 0〜100% を算出。
  - 例: `自信度 = min(100, 件数スコア 60点満点 + カバー率スコア 40点満点)`
- **メーターは記録のたびに目に見えて上がる**(記録直後に「+3% ⤴」のようなアニメーション)。序盤ほど1回の上がり幅を大きくして、最初の数回で気持ちよくさせる。
- 成長演出: 自信度に応じてトリが育つ 🥚 → 🐣 → 🐤 → 🦜(タイトルの「統計トリ」と連動)。
- 「あと◯回記録すると自信アップ!」のような **次の一手の提示** で継続を促す。
- 件数(n)はメーターの詳細表示(タップで展開)に載せる程度にし、メインの画面は数字よりメーターと鳥で直感的に見せる。

## 6. 集計・グラフ表示(切り替え)

タブまたはセレクタで以下を切り替える:

| 表示モード | 内容 |
|-----------|------|
| **時間帯** | 0〜23時(または2時間刻み)ごとの選択肢の割合(積み上げ棒グラフ) |
| **曜日×時間帯** | 曜日を縦・時間帯を横にしたヒートマップ(混んでる率が濃い色) |
| **月の日付** | 1日〜31日ごとの傾向(月初・月末・給料日などの周期を発見できる) |

- 追加候補(v1.x): 曜日のみ / 月別 / 全期間の推移。
- 各グラフで期間フィルタ(全期間 / 直近3ヶ月 / 直近1ヶ月)を選べる。

## 7. データの保守

- **エクスポート / インポート**: 記録一式を JSON(または CSV)でダウンロード・読み込みできる。
  - 端末内保存のため、機種変更・ブラウザデータ消去への備えとして v1 から必須。
  - 将来の共有機能への橋渡しにもなる(ファイルを渡せば手動共有が可能)。

## 8. 共有機能(将来構想・今は案のみ)

数人の仲間内で同じ統計に記録を持ち寄る機能。v1 では実装しない。

### 共有の大前提(めんどくさがり対応)

- **LINE は「URLを送る道具」としてだけ使う。** LINE内ブラウザやLINEミニアプリでアプリを動かすことはしない(タップ数が増えるためNG)。
  - 招待リンクを開いたら「ブラウザで開く → ホーム画面に追加」へ誘導し、以降は本体アプリ(PWA)から直接使う。
  - LINE内ブラウザで開かれた場合は動作はするが、「ホーム画面に追加すると1タップで記録できるよ」と案内を出す。
- **アカウント登録は一切なし。** リンクを開いた瞬間に匿名IDを自動発行して即参加。名前が欲しければニックネームを1つ入れるだけ(それも任意)。

### 案A: 招待リンク方式(本命)

- 統計の「共有」ボタンを押すと招待リンク(URL)を発行。LINE 等で仲間に送る。
- リンクを開いた人は登録なしでそのまま参加でき、全員の記録が合算されて集計・予想に反映される。
- 参加者にも §3.5 のクイック記録URL(0タップ記録)がそのまま使える。
- 必要な追加要素: 小さなAPIサーバーと同期処理 — 具体設計は §11。

### 案B: ファイル手渡し方式(サーバー不要のつなぎ案)

- §7 のエクスポートファイルを送り合い、インポート時に **マージ**(重複記録は除外)する。
- サーバー不要で今の構成のまま実現できるが、リアルタイム性はない。

### 共有時の検討事項(将来)

- 記録の削除・修正権限(自分の記録のみ修正可、など)
- オフライン時の記録と再接続時の同期
- 統計のオーナーによる参加者管理(退出・招待リンクの無効化)

## 9. 画面遷移まとめ

```
ホーム(統計トリ🦜)
 ├─ [新規統計] → 作成画面 → 保存 → ホームに戻る
 ├─ カードのボタン → その場で記録(画面遷移なし)
 └─ カードタップ → 詳細画面
      ├─ 記録ボタン → 記録
      ├─ 予想表示 + 精度自信メーター
      ├─ グラフ(時間帯 / 曜日×時間帯 / 月の日付)
      ├─ 履歴(修正・削除)
      └─ 設定(名前・ボタン編集 / クイック記録URL発行 / エクスポート / 削除 / (将来)共有)

直行URL(ホーム画面アイコン化を想定)
 ├─ #/s/<統計ID>            → 記録ボタン直行画面(1タップで記録)
 └─ #/s/<統計ID>/hit/<選択肢> → 開いた瞬間に記録完了(0タップ)→ 予想+メーター表示
```

## 10. 開発ステップ(案)

| フェーズ | 内容 |
|---------|------|
| v1 | 統計の作成・記録・履歴・集計と予想・**精度自信メーター**・**クイック記録URL(0タップ記録)**(端末内保存) |
| v1.1 | エクスポート/インポート、PWA化(ホーム画面追加・オフライン動作)、成長演出の強化 |
| v1.2 | 予想の精度改善、期間フィルタ、メモ機能 |
| v2 | 共有機能(案A: 招待リンク+クラウド同期)— サーバー設計は §11 |

## 11. サーバー化の設計(v2に向けて)

### 11.1 基本方針 — 「ローカルファースト + 小さな置き場」

サーバーを立てても、**アプリの主役は今のまま端末内(localStorage)**。サーバーは「共有した統計の置き場」だけを担う小さなAPIにする。

- **共有していない統計は今後も端末から一切出ない。** 共有ボタンを押した統計だけがサーバーにコピーされる(プライバシーは初期状態で守られる)。
- **オフラインファースト**: 記録はまずローカルに保存 → ネットがつながっている時に裏で送信。圏外の八百屋でも0タップ記録は今まで通り効く。
- **0タップ記録URLはそのまま**: ページを開く → ローカルに記録 → 裏でサーバーへPOST。送信失敗分はキューに残して次回開いた時に再送。
- アプリ本体は静的PWA(GitHub Pages)のまま。サーバーは別に置き、fetch で話すだけ。

### 11.2 認証 — アカウントなしの「ケーパビリティURL」方式

登録・ログインは一切なし(§8の大前提)。代わりに:

- 共有統計のIDを **推測不可能な長いランダム文字列**(例: 22文字)にし、**「URLを知っている = 参加資格」** とする。
- 端末ごとに匿名の `member_id` を自動発行して localStorage に保持。ニックネームは任意。
- 権限: 記録の追加は参加者全員可 / 記録の修正・削除は**自分の member_id の分だけ** / 統計の設定変更・リンク無効化はオーナー(作成した端末の秘密トークン)のみ。
- リンクが漏れた時は、オーナーが「リンク再発行」で旧URLを無効化できる。

### 11.3 データモデル(サーバー側)

| テーブル | 主な列 |
|---------|--------|
| stats | id(=共有秘密ID), title, choices(JSON), owner_token_hash, created, revoked |
| records | id(クライアント発行UUID), stat_id, choice_idx, ts, member_id, nickname |

- record の id を**クライアント側で発行**するのがポイント。再送しても同じ id なら無視(冪等)になり、v1のインポートマージと同じ「id重複は除外」ルールがそのまま同期アルゴリズムになる。

### 11.4 API(最小5本)

```
POST /stats                     共有開始(statをコピーして招待URLを返す)
GET  /stats/:id                 統計の定義+記録の取得(?since=ts で差分だけ)
POST /stats/:id/records        記録の追加(冪等・複数件まとめ可=再送キュー用)
PATCH/DELETE /stats/:id/records/:rid   自分の記録の修正・削除
POST /stats/:id/revoke         オーナーによるリンク無効化・再発行
```

同期は「開いた時+記録した時に、キューを送って `?since` で差分を取る」だけのポーリング。数人利用ならリアルタイム通信(WebSocket等)は不要。

### 11.5 悪用・事故対策

- IPごとのレート制限(例: 60回/分)、1統計あたりの記録数・サイズ上限、選択肢名の長さ制限。
- URLが実質パスワードなので、招待URLはLINE等のクローズドな場でだけ共有するよう画面に注意書き。
- 定期バックアップ: サーバー側データを毎日ダンプ(こちらで自動化)。

### 11.6 ホスティングの選択肢と推奨

| 選択肢 | 無料枠 | 勝手に止まる? | サーバーコード | あなたの初期作業 |
|--------|-------|--------------|---------------|----------------|
| **Cloudflare Workers + D1(推奨)** | 10万リク/日・十分 | 止まらない | 小さいAPI(全部こちらで書く) | アカウント作成+APIトークンをGitHub Secretsに1回登録(約10分) |
| Firebase Firestore | 十分 | 止まらない | ほぼゼロ(セキュリティルールのみ) | Googleアカウントでプロジェクト作成+設定値の登録(約10分) |
| Supabase | 十分 | **1週間未使用で自動停止**(手動復帰) | ほぼゼロ | アカウント作成(約10分) |
| 自前VPS | 有料 | — | 全部 | 契約・保守(やらない) |

**推奨は Cloudflare Workers + D1**。理由: (1)無料枠で止まらず数人利用には余裕、(2)APIを自作するのでアプリ側は fetch 数行で済み、0タップ再送キューなど細かい挙動を完全に制御できる、(3)デプロイはGitHub Actionsに組み込めて以後の更新も自動。Supabaseは「1週間誰も使わないと止まる」が、たまにしか使わない統計アプリと相性が悪いので外す。

### 11.7 移行ステップ

| 段階 | 内容 | あなたの作業 |
|------|------|-------------|
| v2.0 | サーバー構築+「共有」ボタン(共有開始・招待URL発行・参加・同期) | Cloudflareアカウント作成とトークン登録の1回だけ |
| v2.1 | 誰が押したか表示(ニックネーム)、リンク再発行、参加者数表示 | なし |
| v2.2 | 希望があれば: 自分専用のクラウドバックアップ(共有していない統計の機種変更対策) | なし |
